<?xml version="1.0" encoding="UTF-8" standalone="no"?><project default="main" name="Ant-Test">
        <!-- Sets variables which can later be used. -->
        <!-- The value of a property is accessed via ${} -->
        <property location="/Users/attj/Desktop/code/joe" name="src.dir"/>
        <property location="/Users/attj/Desktop/code/classes" name="build.dir"/>

        <!-- Deletes the existing build, docs and dist directory-->
        <target name="clean">
                <delete dir="${build.dir}"/>
        </target>

        <!-- Creates the  build, docs and dist directory-->
        <target name="makedir">
                <mkdir dir="${build.dir}"/>
        </target>

        <!-- Compiles the java code (including the usage of library for JUnit -->
        <target depends="clean, makedir" name="compile">
                <javac destdir="${build.dir}" srcdir="${src.dir}">
                </javac>

        </target>

        <target depends="compile" name="main">
                <description>Main target</description>
        </target>

</project>